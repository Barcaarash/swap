const http = require("http"); console.log("Testing BNB price endpoint..."); const options = { hostname: "localhost", port: 4053, path: "/api/bnb-price", method: "GET" }; const req = http.request(options, (res) => { let data = ""; res.on("data", (chunk) => { data += chunk; }); res.on("end", () => { console.log("BNB Price Result:", JSON.parse(data)); }); }); req.on("error", (e) => { console.error("Error:", e); }); req.end();
