const express = require('express'); const app = express(); const PORT = 4053; app.get('/api/bnb-price', (req, res) => { console.log('BNB price requested'); res.json({ success: true, price: 586.25 }); }); app.get('/api/token-price', (req, res) => { console.log('Token price requested for:', req.query.address); res.json({ success: true, price: 2.34 }); }); app.listen(PORT, () => { console.log('Simple CoinGecko server running on port', PORT); });
